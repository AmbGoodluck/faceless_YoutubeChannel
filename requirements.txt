# Core
anthropic
requests
edge-tts

# Database
# (sqlite3 is built into Python)

# Image / Video
Pillow
ffmpeg-python

# Renderers (install only what you use)
replicate          # for Replicate renderers (Wan2.1, SadTalker, etc.)
# fal-client       # for fal.ai renderer

# YouTube upload
google-api-python-client
google-auth-httplib2
google-auth-oauthlib

# Utilities
python-dotenv
